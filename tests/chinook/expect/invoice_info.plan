QUERY PLAN [
    SCAN j0,
    SEARCH u0 USING INTEGER PRIMARY KEY (rowid=?) LEFT-JOIN,
    SEARCH u1 USING INTEGER PRIMARY KEY (rowid=?) LEFT-JOIN,
    SEARCH u2 USING INTEGER PRIMARY KEY (rowid=?) LEFT-JOIN,
]
