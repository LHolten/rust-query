QUERY PLAN [
    SCAN _0,
    SEARCH _1 USING INTEGER PRIMARY KEY (rowid=?) LEFT-JOIN,
    SEARCH _2 USING INTEGER PRIMARY KEY (rowid=?) LEFT-JOIN,
    SEARCH _3 USING INTEGER PRIMARY KEY (rowid=?) LEFT-JOIN,
]
