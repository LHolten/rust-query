QUERY PLAN [
    MATERIALIZE a0 [
        SCAN f0,
        SEARCH j0 USING INDEX track_index_1 (genre=?),
    ],
    SCAN j0,
    BLOOM FILTER ON a0 (ff0=?),
    SEARCH a0 USING AUTOMATIC COVERING INDEX (ff0=?) LEFT-JOIN,
]
