QUERY PLAN [
    MATERIALIZE a0 [
        SCAN j0,
        SEARCH f0 USING INTEGER PRIMARY KEY (rowid=?),
        USE TEMP B-TREE FOR GROUP BY,
    ],
    SCAN j0,
    BLOOM FILTER ON a0 (f0=?),
    SEARCH a0 USING AUTOMATIC COVERING INDEX (f0=?) LEFT-JOIN,
]
