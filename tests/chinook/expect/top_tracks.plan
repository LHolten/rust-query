QUERY PLAN [
    MATERIALIZE a0 [
        SCAN f0,
        SEARCH j0 USING COVERING INDEX playlist_track_index_0 (track=?),
    ],
    SCAN j0,
    BLOOM FILTER ON a0 (f0=?),
    SEARCH a0 USING AUTOMATIC COVERING INDEX (f0=?) LEFT-JOIN,
    USE TEMP B-TREE FOR ORDER BY,
]
