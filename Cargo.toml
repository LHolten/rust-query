[workspace]
members = ["rust-query-macros", "fuzz"]

[package]
name = "rust-query"
version = "0.10.0"
edition = "2024"
description = "A query builder using rust concepts."
categories = ["database"]
keywords = ["sqlite", "orm", "schema", "migration"]
repository = "https://github.com/LHolten/rust-query/"
license = "MIT OR Apache-2.0"
rust-version = "1.95"

[[bench]]
name = "tpc-c"
harness = false

[[example]]
name = "axum_tokio"
test = true
[[example]]
name = "blog"
test = true
[[example]]
name = "delete"
test = true
[[example]]
name = "empty_table"
test = true
[[example]]
name = "expense"
test = true
[[example]]
name = "insert_and_select"
test = true
[[example]]
name = "new_blog"
test = true
[[example]]
name = "query_optional"
test = true
[[example]]
name = "queue"
test = true
[[example]]
name = "table_from"
test = true
[[example]]
name = "unchecked"
test = true
[[example]]
name = "jiff"
test = true
harness = false
required-features = ["jiff-02"]

[[test]]
name = "chinook"
required-features = ["jiff-02"]

[dependencies]
rusqlite = { version = "0.40", default-features = false, features = ["cache"] }
k12 = { version = "0.5", optional = true }
rust-query-macros = { path = "rust-query-macros", version = "=0.10.0" }
static_assertions = "1.1.0"
self_cell = "1.3.0"
parking_lot = "0.12.5"
slab = "0.4.12"
annotate-snippets = "0.12.16"
jiff = { version = "0.2.37", optional = true }

[dev-dependencies]
trybuild = "1.0.121"
expect-test = "1"
rand = "0.10.2"
indicatif = "0.18.6"
tokio = { version = "1.53.1", features = ["rt-multi-thread", "macros"] }
axum = "0.8.9"
serde = { version = "1.0.229", features = ["derive"] }

[features]
default = ["base0"]
bundled = ["rusqlite/bundled", "rusqlite/functions"]
dev = ["dep:k12"]
jiff-02 = ["dep:jiff", "rusqlite/functions"]
# this feature is required for everything, it allows making
# anything optional without breaking dependent crates.
base0 = []

# this adds some additional default impls to help run cargo-mutants on rust-query.
# do not enable this for other reasons.
__mutants = []

[package.metadata.docs.rs]
features = ["dev", "jiff-02"]
